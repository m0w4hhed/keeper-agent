// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyA7CqWimnrbEaVxgCN4PoCF5bnAQzNMwaM',
    authDomain: 'nabiilah-duit.firebaseapp.com',
    databaseURL: 'https://nabiilah-duit.firebaseio.com',
    projectId: 'nabiilah-duit',
    storageBucket: 'nabiilah-duit.appspot.com',
    messagingSenderId: '810283488124',
    appId: '1:810283488124:web:600345b10eece1b6'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
